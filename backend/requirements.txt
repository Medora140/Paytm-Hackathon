# Runtime dependencies only. Transitive packages are deliberately not pinned here:
# pip can select versions compatible with the Supabase client and Python runtime.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-dotenv>=1.0,<2.0
python-multipart>=0.0.9,<1.0

# Database, storage, and AI services
supabase>=2.31,<3.0
google-genai>=1.0,<3.0
sentence-transformers>=3.0,<4.0

# Document parsing and OCR
PyMuPDF>=1.24,<2.0
pypdf>=5.0,<7.0
Pillow>=10.0,<12.0
pytesseract>=0.3.10,<1.0

# Numerical operations used by retrieval
numpy>=1.26,<3.0
